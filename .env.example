# Postgres
POSTGRES_USER=petal
POSTGRES_PASSWORD=changeme
POSTGRES_DB=petal

# Public site
SITE_URL=http://localhost
SITE_DOMAIN=localhost

# CORS (comma separated). For single-domain Caddy setup, web origin matches public URL.
CORS_ORIGINS=http://localhost
